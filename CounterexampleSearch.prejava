#include "macros.h"
public class CounterexampleSearch
{
    public void main(String args[])
    {
        if (args.length != 2)
        {
            System.err.println("Usage: "+args[0]+" <lookahead>");
            System.exit(1);
        }
        int lookahead = Integer.parseInt(args[1]);
        PRINT(lookahead);
    }
} // class CounterexampleSearch
